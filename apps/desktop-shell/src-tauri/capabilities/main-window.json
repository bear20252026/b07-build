{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Main Workbench may use only its fixed native commands for direct Provider communication and workspace selection; no general shell, filesystem, dialog plugin, autostart, or native-helper permission is granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
