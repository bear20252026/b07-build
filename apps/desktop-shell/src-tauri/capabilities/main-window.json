{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability for the bundled local Workbench window; no privileged plugins are enabled.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
