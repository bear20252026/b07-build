{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Main Workbench window may request only the fixed bundled local Gateway serve action; no general shell, filesystem, autostart, or native-helper permission is granted.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/awo-runtime-gateway",
          "sidecar": true,
          "args": [
            "serve"
          ]
        }
      ]
    }
  ]
}
