# 桌面首页 UI 对齐任务

- [x] 保持现有直接 Provider API 连接、模型选择、会话恢复与流式对话行为不变。
- [x] 审查首页组件、事件绑定与现有 Unsloth 风格参考边界。
- [x] 只改造首页视觉为薄荷背景、圆角桌面窗口、轻量侧栏与居中对话输入区。
- [x] 已完成类型检查、Workbench 生产构建、全量 305 项测试与 Rust 编译检查；现有直接 Provider、模型选择与流式调用代码未改动，待 Windows 本机安装器构建复核。
- [x] 复核真实桌面首页在已连接模型后的首屏层级与 Unsloth 参考图差异。
- [x] 继续精简首页视觉密度，突出会话主画布与实际输入框，不增加任何新功能。
- [x] 已完成类型检查、Workbench 生产构建、全量 305 项测试、Rust 编译和 Windows 本机安装器构建；直接 Provider 流式会话入口未改动。
- [x] 归纳用户提供参考图与 Apple 设计语言的首页特征，明确恢复白灰蓝与黑灰蓝色彩体系。
- [x] 制定删除薄荷绿背景、绿色描边及无关首页卡片的视觉改造清单，不新增功能。
- [ ] 取得用户对首页“对话区域占据大半屏”的具体改造方案确认后再实施。
- [x] 已取得用户对首页“对话区域占据大半屏”的具体改造方案确认并实施。
- [x] 移除首页薄荷绿背景、绿色描边和绿色操作强调，恢复 Apple 白灰蓝 / 黑灰蓝 token。
- [x] 统一首页窗口、侧栏、输入框与按需弹层的圆角、阴影和中性分层。
- [x] 将首页未对话与已对话状态收束为对话主画布，保留设置与现有功能入口但不堆叠首屏。
- [x] 完成视觉回归、直接 Provider 会话时间线验证与 Windows 本机安装器构建；交付清单已核验为 de86aac、无 Gateway、NotSigned。
- [x] 诊断首页任务发送是否正确调用直接 Provider 流式对话，而非遗留任务执行链路。
- [x] 修复“新建项目”事件、项目持久化与项目页面切换回归。
- [x] 删除首页白色外框，确保未对话和已对话状态在桌面首屏内完整可见且无需页面滚动。
- [x] 对照 OpenWorker 与 AtomCode 的 API key / Provider 会话模式记录差异，仅采用已验证的交互修复。
- [x] 已诊断首页发送回落到遗留任务链路、项目创建被旧 Gateway 开关禁用的根因；修复说明已保存。
- [x] 完成自动化测试、桌面交互验证与 Rust 编译检查；待基于本次修复构建 Windows 本机安装器。
- [x] 持久化完整的直接 Provider 账户配置，并在 Windows 桌面应用启动时仅重建原生内存会话，不自动发起供应商网络请求。
- [x] 为 Provider 账户恢复增加本地账本测试；全仓 309/309 测试、类型检查、Workbench 构建与 Rust 编译检查通过。
- [x] 调研 OpenWorker 与 AtomCode 的 Provider、会话、多项目、流式过程与工具调用模式，并形成与当前桌面端的差距清单和版权边界记录。
- [x] 实现首页输入开始即将欢迎与右侧上下文卡片向上退出、由沉浸式对话主画布接管的交互，不改变第三方 Provider 直连链路。
- [x] 为对话消息增加可折叠的过程/工具活动呈现结构；仅显示供应商实际返回或本地实际执行的元数据，绝不伪造模型思考内容。
- [ ] 制定第三方模型联网搜索能力的 Provider-native 与本地工具两条可选路线，明确所需供应商能力、授权与可审计边界，未经确认不启用外部搜索执行。
- [x] 诊断直接聊天只发送最后一个 prompt、未将同一会话历史传给 Provider 的根因，并改为按当前会话组织历史上下文。
- [x] 将本地会话从单一“当前对话”升级为可在左侧创建、切换、重命名、删除和按项目筛选的多会话目录；保留用户既有本地历史。
- [x] 支持将会话归属到本地项目，并在左侧项目区域内展示和切换相应会话，避免项目、任务和聊天状态互相覆盖。
- [ ] 按 MiMo 官方 OpenAI-compatible Web Search 协议增加显式启用、搜索活动、可点击引用与搜索用量记录；只对已开通 MiMo 插件的账户发送该工具字段。
- [ ] 为历史上下文、多会话切换、项目归属、真实 reasoning 折叠与 MiMo 搜索事件补充自动化及 Windows 本机验证。
- [x] 核验 AtomCode 的实际联网检索、Web 工具、MCP 与供应商原生搜索调用链，避免将其能力错误归因于 MiMo 插件。
- [x] 设计并实现独立于 MiMo 插件的可选搜索工具路径；与 MiMo Provider-native 搜索并存、各自显式启用并保留来源与成本边界。
- [x] 为历史上下文、多会话切换、项目归属、真实 reasoning 折叠与独立搜索事件补充自动化和本地预览验证；MiMo Provider-native 搜索另行保留为后续兼容项。
- [x] 阅读并核验 last30days-skill、last30days-skill-cn、OpenWorker 与 AtomCode 的公开联网检索、文件上下文和工具调用模式，记录许可证与可复用职责边界。
- [x] 定位独立联网搜索按钮无有效状态反馈或搜索结果不可用的根因，改为可见的准备、执行、完成、无结果与失败状态，并使用清晰苹果蓝开关反馈。
- [x] 修复聊天附件仅保留描述符、没有将可安全读取的文本内容传给模型的链路；对二进制或大型文件保留明确限制和可见原因。
- [x] 复核并修复同一会话的模型回复在下一轮后消失、历史不进入 Provider 或跨项目混显的问题；在支持范围内保留最长 1M 字符的会话上下文预算。
- [x] 实现用户显式触发、可暂停/取消、可查看输出的 Windows 本地终端执行能力；普通本地命令不设无效门槛，但管理员级、破坏性或外部副作用操作仍要求最后确认。
- [ ] 为搜索状态、附件上下文、1M 会话历史和终端执行增加自动化、桌面端验证及 Windows 本机构建回归。
- [x] 将联网检索从“摘要唯一上下文”改为“网页原始可读内容 + 标题 + URL 直接进入本轮 Provider 请求”；按所选模型实际上下文窗口显式报告装入范围，禁止静默截断来源。
- [ ] 评估用户提供的 Searchpin、国内 MCP、SearXNG 混合检索和 last30days 工具调用方案；仅采用经核验且许可、部署方式与 Windows 桌面端兼容的职责模式。
- [x] 当输入超过阈值时自动生成带来源标记的 TXT 上下文文件，并把其正文与原始问题传给本轮 Provider；用户可看到该文件已纳入模型上下文。
- [ ] 为代码、文本、JSON、Markdown、PDF、Office、压缩包与图片增加真实的提取/多模态传递状态；不能解析或当前模型不支持的格式必须如实显示，而非本地静默丢弃。
- [x] 扩展模型请求契约以按 Provider 能力传递图像，保留图片内容而非仅保留文件名；在不支持视觉的 Provider 上保留可切换的文本提取路径。
- [ ] 将模型工具调用（搜索请求 → 本地执行 → 原始结果回传 → 最终回答）设计为明确可见的会话活动，不依赖单一 MiMo 插件或单一搜索后端。
