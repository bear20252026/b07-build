# 桌面首页 UI 对齐任务

- [x] 保持现有直接 Provider API 连接、模型选择、会话恢复与流式对话行为不变。
- [x] 审查首页组件、事件绑定与现有 Unsloth 风格参考边界。
- [x] 只改造首页视觉为薄荷背景、圆角桌面窗口、轻量侧栏与居中对话输入区。
- [x] 已完成类型检查、Workbench 生产构建、全量 305 项测试与 Rust 编译检查；现有直接 Provider、模型选择与流式调用代码未改动，待 Windows 本机安装器构建复核。
- [x] 复核真实桌面首页在已连接模型后的首屏层级与 Unsloth 参考图差异。
- [x] 继续精简首页视觉密度，突出会话主画布与实际输入框，不增加任何新功能。
- [x] 已完成类型检查、Workbench 生产构建、全量 305 项测试、Rust 编译和 Windows 本机安装器构建；直接 Provider 流式会话入口未改动。
- [x] 归纳用户提供参考图与 Apple 设计语言的首页特征，明确恢复白灰蓝与黑灰蓝色彩体系。
- [x] 制定删除薄荷绿背景、绿色描边及无关首页卡片的视觉改造清单，不新增功能。
- [ ] 取得用户对首页“对话区域占据大半屏”的具体改造方案确认后再实施。
- [x] 已取得用户对首页“对话区域占据大半屏”的具体改造方案确认并实施。
- [x] 移除首页薄荷绿背景、绿色描边和绿色操作强调，恢复 Apple 白灰蓝 / 黑灰蓝 token。
- [x] 统一首页窗口、侧栏、输入框与按需弹层的圆角、阴影和中性分层。
- [x] 将首页未对话与已对话状态收束为对话主画布，保留设置与现有功能入口但不堆叠首屏。
- [x] 完成视觉回归、直接 Provider 会话时间线验证与 Windows 本机安装器构建；交付清单已核验为 de86aac、无 Gateway、NotSigned。
- [x] 诊断首页任务发送是否正确调用直接 Provider 流式对话，而非遗留任务执行链路。
- [x] 修复“新建项目”事件、项目持久化与项目页面切换回归。
- [x] 删除首页白色外框，确保未对话和已对话状态在桌面首屏内完整可见且无需页面滚动。
- [x] 对照 OpenWorker 与 AtomCode 的 API key / Provider 会话模式记录差异，仅采用已验证的交互修复。
- [x] 已诊断首页发送回落到遗留任务链路、项目创建被旧 Gateway 开关禁用的根因；修复说明已保存。
- [x] 完成自动化测试、桌面交互验证与 Rust 编译检查；待基于本次修复构建 Windows 本机安装器。
- [x] 持久化完整的直接 Provider 账户配置，并在 Windows 桌面应用启动时仅重建原生内存会话，不自动发起供应商网络请求。
- [x] 为 Provider 账户恢复增加本地账本测试；全仓 309/309 测试、类型检查、Workbench 构建与 Rust 编译检查通过。
- [x] 调研 OpenWorker 与 AtomCode 的 Provider、会话、多项目、流式过程与工具调用模式，并形成与当前桌面端的差距清单和版权边界记录。
- [x] 实现首页输入开始即将欢迎与右侧上下文卡片向上退出、由沉浸式对话主画布接管的交互，不改变第三方 Provider 直连链路。
- [x] 为对话消息增加可折叠的过程/工具活动呈现结构；仅显示供应商实际返回或本地实际执行的元数据，绝不伪造模型思考内容。
- [ ] 制定第三方模型联网搜索能力的 Provider-native 与本地工具两条可选路线，明确所需供应商能力、授权与可审计边界，未经确认不启用外部搜索执行。
- [x] 诊断直接聊天只发送最后一个 prompt、未将同一会话历史传给 Provider 的根因，并改为按当前会话组织历史上下文。
- [x] 将本地会话从单一“当前对话”升级为可在左侧创建、切换、重命名、删除和按项目筛选的多会话目录；保留用户既有本地历史。
- [x] 支持将会话归属到本地项目，并在左侧项目区域内展示和切换相应会话，避免项目、任务和聊天状态互相覆盖。
- [ ] 按 MiMo 官方 OpenAI-compatible Web Search 协议增加显式启用、搜索活动、可点击引用与搜索用量记录；只对已开通 MiMo 插件的账户发送该工具字段。
- [ ] 为历史上下文、多会话切换、项目归属、真实 reasoning 折叠与 MiMo 搜索事件补充自动化及 Windows 本机验证。
- [x] 核验 AtomCode 的实际联网检索、Web 工具、MCP 与供应商原生搜索调用链，避免将其能力错误归因于 MiMo 插件。
- [x] 设计并实现独立于 MiMo 插件的可选搜索工具路径；与 MiMo Provider-native 搜索并存、各自显式启用并保留来源与成本边界。
- [x] 为历史上下文、多会话切换、项目归属、真实 reasoning 折叠与独立搜索事件补充自动化和本地预览验证；MiMo Provider-native 搜索另行保留为后续兼容项。
- [x] 阅读并核验 last30days-skill、last30days-skill-cn、OpenWorker 与 AtomCode 的公开联网检索、文件上下文和工具调用模式，记录许可证与可复用职责边界。
- [x] 定位独立联网搜索按钮无有效状态反馈或搜索结果不可用的根因，改为可见的准备、执行、完成、无结果与失败状态，并使用清晰苹果蓝开关反馈。
- [x] 修复聊天附件仅保留描述符、没有将可安全读取的文本内容传给模型的链路；对二进制或大型文件保留明确限制和可见原因。
- [x] 复核并修复同一会话的模型回复在下一轮后消失、历史不进入 Provider 或跨项目混显的问题；在支持范围内保留最长 1M 字符的会话上下文预算。
- [x] 实现用户显式触发、可暂停/取消、可查看输出的 Windows 本地终端执行能力；普通本地命令不设无效门槛，但管理员级、破坏性或外部副作用操作仍要求最后确认。
- [ ] 为搜索状态、附件上下文、1M 会话历史和终端执行增加自动化、桌面端验证及 Windows 本机构建回归。
- [x] 将联网检索从“摘要唯一上下文”改为“网页原始可读内容 + 标题 + URL 直接进入本轮 Provider 请求”；按所选模型实际上下文窗口显式报告装入范围，禁止静默截断来源。
- [ ] 评估用户提供的 Searchpin、国内 MCP、SearXNG 混合检索和 last30days 工具调用方案；仅采用经核验且许可、部署方式与 Windows 桌面端兼容的职责模式。
- [x] 当输入超过阈值时自动生成带来源标记的 TXT 上下文文件，并把其正文与原始问题传给本轮 Provider；用户可看到该文件已纳入模型上下文。
- [ ] 为代码、文本、JSON、Markdown、PDF、Office、压缩包与图片增加真实的提取/多模态传递状态；不能解析或当前模型不支持的格式必须如实显示，而非本地静默丢弃。
- [x] 扩展模型请求契约以按 Provider 能力传递图像，保留图片内容而非仅保留文件名；在不支持视觉的 Provider 上保留可切换的文本提取路径。
- [ ] 将模型工具调用（搜索请求 → 本地执行 → 原始结果回传 → 最终回答）设计为明确可见的会话活动，不依赖单一 MiMo 插件或单一搜索后端。
- [x] 以固定上游提交将 mvanhorn/last30days-skill 与 Jesseovo/last30days-skill-cn 的完整源码、原始 MIT 许可证、版权声明和上游元数据纳入 third_party/，禁止散落复制或移除文件头。
- [x] 建立 THIRD_PARTY_NOTICES.md、第三方版本锁定表和可复现同步脚本，记录两个 last30days 上游项目及全部新增嵌入组件的来源、许可证、提交号和本地修改。
- [x] 在聊天输入区增加用户显式的“近 30 天研究 / 近 30 天中文研究”触发方式，并将原始研究结果、来源和诊断作为可折叠会话活动传入当前 Provider。
- [x] 调研并记录 SearXNG 固定版本、AGPL-3.0 义务、Windows 可再分发 Python 运行时、网络服务生命周期和安装器体积影响；已完成完整合规说明、loopback JSON 搜索验证和 Windows 本机安装器构建。
- [ ] 建立混合检索注册表，首批固定并行输入为 Exa MCP、last30days 国际研究器、last30days 中文研究器与本地 SearXNG；后续搜索后端必须以独立适配器、来源声明、许可证和 Windows 运行时检测方式加入。
- [x] 实现用户明确选择的“混合检索”模式，并行执行 Exa、本地 SearXNG、last30days 国际和 last30days 中文后端，记录每个后端的成功或失败、来源数量与原始结果。
- [x] 对混合结果按规范化 URL 去重，保留后端归属、原始正文和可点击来源，并在 1M Provider 请求预算内明确报告未装入的结果，禁止静默截断。
- [x] 固定并完整纳入 SearXNG 源码、AGPL-3.0 文本、作者声明、Python 运行时及依赖许可证；实现仅绑定 loopback 的启动、健康检查、停止与本地 HTTP 适配器后，再加入混合检索并构建 Windows 安装器。
- [ ] 复现并修复同一会话下一轮输入后历史消息在聊天时间线中消失的问题，确保持久账本、活动会话状态和渲染键值在发送、流式更新、切换与重启后保持一致。
- [ ] 将网页原始上下文来源上限扩展为每轮最多 100 条，同时以可配置的来源正文与总字符预算控制 Provider 请求，并在界面中显示已装入、去重、跳过和失败的真实数量；100 条传递与预算已实现，统计展示待补。
- [ ] 修复应用内搜索启动/停止与错误呈现，禁止为本地 SearXNG、Exa 或研究后端弹出可见终端窗口；已实现 Windows CREATE_NO_WINDOW 与标准输出隐藏，待 Windows 实机回归。
- [ ] 让左侧项目/会话/设置区域作为独立固定滚动容器，聊天主时间线独立滚动，避免整个应用页面共同滚动或侧栏随消息滑走；布局实现待 Windows 桌面视觉验证。
- [ ] 为聊天时间线实现按需渲染或虚拟滚动、流式更新节流、消息/附件缓存上限和会话切换时的事件监听清理，建立可测量的内存生命周期与泄漏回归测试；最近 60 条窗口与 50ms 流式节流已实现，附件缓存/实际堆快照回归待补。
- [ ] 引入可访问且按需加载的 KaTeX 数学公式渲染，支持复杂块级/行内 LaTeX、转义符号与错误回退，不将公式原样作为不可读文本显示；实现和构建验证已完成，待 Windows 桌面公式样例回归。
- [ ] 复现并修复“Provider 连接测试成功但首页实际聊天报第三方服务未响应”的差异：统一测试、会话恢复与 SSE 聊天的 Base URL、协议、认证头、模型、超时和错误回执，并增加链路一致性自动化测试。
- [x] 复现并修复 Windows 安装版启动后白屏的问题：WebView2 已捕获 `App.tsx` 的 `useRef is not defined` 启动期异常；补齐 React 导入并通过 Workbench 类型检查与生产构建。
- [ ] 将此前已提出的核心需求整理为可追溯验收矩阵，区分已实现、待实机验证、尚未实现与不应承诺的项目，避免遗漏或将未完成能力表述为已交付。
- [ ] 对修复后的桌面端执行真实 Provider 聊天、会话历史、多会话切换、100 来源搜索、文件上下文、终端、固定侧栏、长会话内存和 KaTeX 公式的 Windows 回归测试。
- [x] 仅在白屏修复与回归测试通过后，使用 Windows 本机工具链重新构建、核验并交付新的安装器及其来源提交清单（0.1.1，SHA-256 待发布清单核验）。
- [x] 停止将 `C:\Users\17296\AppData\Local\AI Work OS\awo-desktop-shell.exe` 的旧 Gateway/任务工作台界面当作当前版本；仅以 `C:\Users\17296\Downloads\AI Work OS_0.1.0_x64-setup (10).exe` 及其安装结果作为白屏诊断对象。
- [x] 对目标 Downloads 安装包计算哈希、提取版本与打包资源证据，确认其与哪个源码提交和安装器清单对应，再复现其实际白屏行为。
- [ ] 白屏修复后逐项确认首页确为当前直接 Provider 聊天首页，不含淘汰的 Gateway/任务工作台首屏，并将此作为新安装器发布前的阻断性验收项。
- [x] 修复 Windows PowerShell 下根 `npm test` 使用 Unix `find` 命令而将 `packages`、`apps` 目录错误传入 Node 测试运行器的问题，并在 Windows 本机跑通完整测试（322/322 通过）。
- [x] 修复 Windows 文件系统不允许 `delivery:<hash>.zip` 冒号路径导致的交付 ZIP 创建失败，并保持现有交付标识与哈希校验语义。
- [x] 修复 Gateway HTTP 测试在 Windows 临时目录清理时出现的 `EPERM` 锁定问题，确保测试结束前完全释放监听器、SQLite 文件和文件句柄。
- [x] 将桌面应用、Tauri/NSIS 元数据、安装器文件名、发布清单与更新说明统一升级为新的语义化版本，建立每次发布必须递增版本号的自动化校验（下一发布版本：0.1.1）。
- [ ] 审计并从正式桌面运行路径移除无用 Gateway 依赖；保留仅仍被明确需要的独立开发/测试模块，并让普通聊天、搜索、项目和设置不显示或依赖 Gateway。
- [ ] 研究并可追溯复用 AionUi 的三栏工作区交互：稳定会话侧栏、可独立开关的右侧文件/变更检查器、中央聊天独立滚动与底部输入区；保留上游版权、许可证和修改说明。
- [ ] 修复联网搜索期间的桌面白屏/崩溃路径，增加搜索任务取消、错误边界、事件清理和可诊断的应用内状态，不弹出系统终端。
- [x] 恢复聊天时间线在固定布局内的鼠标滚轮、触控板、触控拖动与键盘滚动，并确保流式接收时不阻塞用户查看历史。
- [x] 降低模型流式回答造成的页面卡顿：审计渲染提交、滚动、公式与来源卡片开销，补齐节流、按需渲染和事件释放的回归测试（首页入口从约 808 KB 降至 364 KB，次级工作面按需加载）。
- [ ] 增加项目级持久记忆文件：可由主人明确编辑或由 AI 在可审查规则下追加，跨会话恢复并在发送时按预算注入直接 Provider 上下文。
- [x] 增加本地 GitHub 个人访问令牌配置、仓库选择、变更预览、提交与显式确认推送流程；令牌不得进入日志、聊天、Provider 上下文、版本库或发布清单。
- [x] 修复图片发送出现 `provider-http-404` 时的诊断：图片内容仍传递至第三方 Provider；若上游返回 404，应用明确提示当前 Base URL、协议或模型未接受视觉内容，而不误报为本地拦截。
- [x] 修复嵌入式本地 SearXNG 健康检查可能因缺少 loopback 请求地址而超时的问题：健康检查与搜索请求显式标记 `127.0.0.1`，Windows 冷启动预算扩展至 35 秒，保持无终端窗口。
- [x] 修复下一轮消息到达时聊天虚拟窗口强制跳回最近 60 条的问题，保留用户已加载和正在阅读的历史窗口，并在切换会话时才重置窗口。
- [x] 为每条用户/助手消息加入一键复制全文按钮，并加入仅在用户远离底部时出现的“跳到最新消息”向下按钮；自动跟随不会抢夺上滚阅读位置。
- [ ] 将聊天中的“准备上传代码”意图接入本机 GitHub 协作执行器，使 AI 可展示变更、草拟提交说明并在主人确认后调用本机令牌提交推送；令牌字符串不进入模型文本。
- [x] 云端 Windows 安装器工作流不再构建旧 Gateway sidecar，且从桌面包的唯一版本元数据生成工件清单。
- [ ] 将本轮修复以新的 0.1.2 云端 Windows 安装器工件构建、核验并交付，明确区别于 0.1.1。
- [ ] 将图片、SearXNG、历史与聊天交互修复发布为新的 0.1.2 版本，并以云端 Windows 构建工件与 0.1.1 明确区分。
- [x] 对照已提及的 OpenWorker、AtomCode、AionUi、Chatbox、Cherry Studio、LobeChat、AnythingLLM、Jan、Open WebUI、OpenClaw、ClawCode、UI-TARS Desktop、DeepSeek-Harness 与 MiMo-Code，形成基于一手资料的能力矩阵、许可边界与适配结论。
- [x] 将 AI Work OS 当前实现映射到成熟项目能力矩阵，区分“已具备”“已实现待 Windows 验证”“尚缺失”“不宜复制或不适合当前 Windows 桌面范围”。
- [x] 基于用户价值、可靠性、依赖关系、维护成本和许可证风险，形成 P0–P3 改进路线；未经主人确认不将建议性新功能直接纳入实现。
- [x] 实现统一 Provider 连接诊断：连接测试、实际聊天、流式首字节、图片能力与错误分类使用同一配置快照，并且不记录或回传密钥。
- [x] 在设置中提供可复制的本地诊断报告，包含桌面版本、来源提交、Provider 非敏感配置、SearXNG 状态、工作区/会话存储和最近错误分类。
- [x] 为聊天输入接入确定性 `/github` 协作命令和可见提示；仅打开本地 GitHub 协作面板及加载状态，提交和推送仍必须由面板勾选确认。
- [x] 建立 0.1.3 Windows 回归清单与自动化覆盖，至少包括真实聊天、图片、SearXNG、混合检索、长会话、复制、跳到最新、KaTeX、会话切换和 GitHub 协作入口。
- [x] 通过云端 GitHub Actions Windows runner 构建、来源证明核验并交付新的 0.1.3 安装器；不得使用用户 Windows 本机构建工具链。运行 #32650679277 已成功，来源提交为 8c251051910687441fe4942f7d8d7a09b678167f。
- [x] 阻断性修复：排查 0.1.3 第三方 Provider 再次显示“无法连接”的恢复、配置、连接测试和首页真实聊天链路；普通聊天必须继续原生直连，禁止通过重新引入 Gateway 规避故障。0.1.4 使预设或自定义 MiMo Token Plan 的 `tp-` 密钥均使用官方 `api-key` 认证头。
- [x] 将 `provider-request-failed` 从笼统“第三方服务未响应”拆分为 DNS、TLS/证书、连接超时、请求超时和无法建立连接等不含密钥的可操作错误分类，并核验 MiMo Token Plan 中国区直连请求构造。
- [x] 通过 GitHub Windows runner 构建并核验 0.1.4 直连修复安装器：工作流 #32652865337 成功，来源提交 9e2eba7f52a341ca80c4969f2039f9bdaaf03758，`bundlesExplicitGatewaySidecar=false`。
- [x] 阻断性修复：依据 MiMo 官方文档核验并实现视觉模型能力路由、OpenAI/Anthropic 图片载荷和自由模型切换；图片不得被静默丢弃，也不得因固定模型名或错误端点误报为本地限制。0.1.5 新增 MiMo `mimo-v2.5` 视觉模型快捷选择和 `mimo-v2.5-pro` 图片专属错误说明。
- [x] 阻断性修复：隔离 SearXNG、Exa、混合检索与 Provider 聊天错误；检索后端失败必须给出各自可读状态，并继续发送原始用户问题至选中的第三方模型。0.1.5 将嵌入 Python 早退、loopback 请求失败和拒绝状态分别显示为检索活动。
- [x] 通过 GitHub Windows runner 构建并核验 0.1.5 图片与检索修复安装器：工作流 #32654528364 成功，来源提交 9c52231b6ca2870de8aa4c60183c9b6548fcb516，`bundlesExplicitGatewaySidecar=false`。
- [x] 最高优先级阻断性修复：Windows 0.1.5 诊断已确认 Provider 配置成功但原生 HTTPS 在 691–720 ms 内 `provider-connect-failed`；对照 AtomCode 的系统证书、系统代理与 Windows HTTP 客户端实现修复基础直连，不得以 Gateway 取代。0.1.6 在 Windows 启用 SChannel，并只为 Provider 原生 HTTPS 客户端透明使用已启用的系统代理；工作流 #32655908525 已完成来源核验。
- [x] 调研成熟 AI 产品在 Provider 可靠性、模型能力管理、搜索/RAG、项目记忆、代码协作、可观测性、桌面体验与跨端演进方面的近期实践，并形成一手资料记录。
- [x] 将 AI Work OS 当前能力映射至成熟产品矩阵，区分已具备、已实现待 Windows 实测、阻断性缺口、可选增强及不适合当前范围的方向。
- [x] 形成 P0–P3 后续路线图，说明每项用户价值、工程依赖、验收标准和许可证边界；未经主人确认不自动实施建议性新功能。
- [x] 在首页聊天区实现跨厂商 Provider 连接与模型自由切换：显示当前连接、协议和已知能力，保留任意手填模型，不自动替换用户选择，不经过 Gateway。
- [x] 实现 Connection Center v2：以同一 trace 关联连接测试、流式测试与真实聊天，展示无密钥的网络/认证/模型/图片/首 token/流中断时间线。
- [x] 实现 Search Run Card：按轮显示 SearXNG、Exa、抓取和重排状态、来源数、失败后端、重试入口和发送给模型的来源清单；检索失败仍续发普通聊天。
- [x] 新建 0.1.7 Windows 实机回归矩阵，覆盖跨厂商/模型切换、图片、系统代理、SearXNG 与检索失败隔离；通过云端 Windows runner 构建及来源核验交付安装器。工作流 #32714330735 成功，来源提交 237b2e42fd4eb94c9b11187cbabbf274f60643ce，`bundlesExplicitGatewaySidecar=false`。
- [x] 实现白盒项目记忆：AI/主人只能提出 `AI_WORK_OS_MEMORY.md` 的可见追加差异，打开面板不读文件，主人读取、预览、勾选批准后才写入；记忆继续作为有界项目上下文而非不可见提示词。
- [x] 实现上下文预算面板：显示已选模型、本轮会话/记忆/附件元数据的本地字符预算与裁剪状态；记忆只在显式点击后读取，明确不是供应商 token、费用或账单。
- [x] 实现会话检查点、从消息分支与 Markdown/JSON 导出，保持原会话不被改写、恢复操作创建新会话，并排除隐藏 Provider context 与活动详情。
- [x] 扩展 GitHub 协作面板：提交前显示本地状态/变更统计与显式触发的固定 `git diff --check` 测试摘要，保留提交说明草案和显式 push 确认；PAT 不进入模型或日志。
- [x] 实现持久化本地调用量与运行账本：按 Provider、模型和会话汇总调用数、首 token、总耗时、错误类别和可见输出字符；最多保留 128 条脱敏元数据，不读取或保存聊天正文、端点、密钥、供应商 token 或费用。
- [x] 统一 P1 的 Apple 风格视觉、交互状态、键盘可达性与深浅主题，保持首页一屏优先、浅灰材质、克制蓝色和精致层级；长会话保留可展开的跨厂商/模型显式切换。
- [x] 升级至 0.1.8，通过全量自动化验证与 GitHub Windows runner 云端构建、工件哈希和来源证明核验后交付；不得使用用户 Windows 本机构建工具链。
- [x] 打磨聊天首页与单内容 Inspector 的 Apple 风格视觉层级、材质、留白、状态反馈和轻量微交互；不得改变直接 Provider、项目记忆、检查点、GitHub 协作或本地账本的数据与权限边界。
- [x] 验证视觉升级后的类型检查、生产构建、性能预算、全量 Node 测试与补丁格式；浏览器预览因不具备 Tauri 原生桥接而未渲染真实桌面首页，最终桌面与窄屏视觉回归保留为下一次云端 Windows 安装器验收项。
- [x] 升级至 0.1.9，通过 GitHub Windows runner 云端构建、工件哈希与来源证明核验交付 Apple 风格视觉升级；不得使用用户 Windows 本机构建工具链。
- [x] P1.5：实现本地、可选、脱敏的会话性能观察，展示渲染/持久化/流式节流等聚合指标；不得记录提示词、回复、图片、密钥、Base URL、代理或工作区路径。
- [x] P1.5：审查并优化长会话窗口化渲染、滚动恢复、流式资源释放与本地持久化上限，避免影响已完成会话、检查点、分支和导出边界。
- [x] P1.5：完善 Windows 实机回归流程，覆盖系统代理下直连 Provider、SSE 流式、深浅主题、减少动态、长会话、检查点、导出和 GitHub 预检；真实结果仅由用户验收记录。
- [x] P2：实现项目工作台基础，将项目、会话、显式选择的工作区文件、产物预览与受确认的终端计划进行可见关联；不自动扫描、导入、上传或执行。
- [x] P2：实现本地知识库基础，支持主人明确选择文件后本地索引、可见索引状态、检索引用卡片和来源预览；不得自动上传原始文件或将未选择资料发送给 Provider。
- [x] 升级至 0.1.10，通过全量自动化验证与 GitHub Windows runner 云端构建、工件哈希和来源证明核验后交付 P1.5/P2 基础；不得使用用户 Windows 本机构建工具链。
- [x] 0.1.11：扩展本地性能健康摘要，明确区分 UI 帧响应、本地持久化、流式调度与 Provider 网络延迟；不得保存或导出正文、图片、密钥、模型、Base URL、代理、文件名或路径。
- [x] 0.1.11：优化长会话的资源生命周期，包括可见消息窗口、滚动状态、公式按需渲染、附件临时 URL 与 Inspector 订阅/动画帧释放；不得丢失历史、图片、检查点、分支或导出能力。
- [x] 0.1.11：建立仅使用 transform/opacity、可中断且遵从减少动态偏好的 Apple 风格动效基础；不得用高成本布局/滤镜动画干扰聊天滚动或 SSE 流式输出。
- [x] 升级至 0.1.11，通过全量自动化验证与 GitHub Windows runner 云端构建、工件哈希和来源证明核验后交付；不得使用用户 Windows 本机构建工具链。
- [x] 修复首页模型切换回归：连接下拉、模型输入、当前会话选择与 Tauri 原生请求必须使用同一份用户显式 Provider/模型选择；覆盖 DeepSeek、龙猫、MiMo 与任意自定义 OpenAI/Anthropic 兼容连接，禁止静默回退到 MiMo 或 Gateway。
- [x] 为跨厂商新会话、既有会话切换、模型自填和实际原生请求参数补充回归测试；发布 Windows 修复版本前完成全量验证、来源核验和实机验收清单。
- [x] 升级至 0.1.12，通过全量自动化验证与 GitHub Windows runner 云端构建、工件哈希和来源证明核验后交付首页跨厂商模型切换修复；不得使用用户 Windows 本机构建工具链。
- [x] 修复聊天页面流式对话中的状态、滚动和渲染稳定性：防止高频更新造成页面抖动、滚动跳跃、局部状态重置或重复帧调度；不得改变 Tauri 原生 Provider 请求、模型选择、会话历史或附件传递。
- [x] 重构首页与高频聊天 UI 为圆润、统一、具有明确层级的 Apple 风格：统一曲率、间距、按钮、消息气泡、输入区和 Inspector 视觉语言；轻量动效必须可中断、尊重减少动态偏好且不阻塞 SSE 或滚动。
- [x] 为稳定性和圆润 UI 重构补充回归测试、深浅主题/窄屏验收清单与性能预算验证；浏览器预览缺少 Tauri 原生桥接，最终桌面视觉和流式回归保留为 Windows 实机验收项。
- [ ] 升级至 0.1.13，通过全量自动化验证与 GitHub Windows runner 云端构建、工件哈希和来源证明核验后交付聊天稳定性与圆润 UI 重构；不得使用用户 Windows 本机构建工具链。
